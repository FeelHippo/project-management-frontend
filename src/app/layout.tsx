import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';
import React from 'react';
import { SupertokensInit } from '@/components/supertokensInit';
import { Toaster } from 'sonner';
import { AppContextProvider } from '@/app/providers/context';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

// TODO(Filippo): SupertokensInit re-renders the entire app on authentication changes
// this makes AppContextProvider lose its context. Try to fix it.
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <SupertokensInit>
        <body
          className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        >
          <AppContextProvider>{children}</AppContextProvider>
          <Toaster />
        </body>
      </SupertokensInit>
    </html>
  );
}
